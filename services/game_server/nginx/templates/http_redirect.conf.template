server {
    listen 80;
    listen [::]:80;
    server_name $NGINX_HOST;

    # ACME challenge for Let's Encrypt (webroot)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$NGINX_HOST$request_uri;
    }
}
